<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Event</title>
    <link rel="stylesheet" href="../css/editevent.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .form-container {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 80%;
            max-width: 600px;
        }
        h2 {
            text-align: center;
            color: #333;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
            color: #333;
        }
        input[type="text"],
        input[type="date"],
        input[type="time"],
        input[type="number"],
        textarea,
        select {
            width: 100%;
            padding: 8px;
            margin-top: 2px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        button {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Edit Event</h2>
        <form action="/events/<%= event._id %>/edit" method="POST">
            <div class="form-group">
                <label for="name">Event Name:</label>
                <input type="text" id="name" name="name" value="<%= event.name %>" required>
            </div>
            <div class="form-group">
                <label for="description">Description:</label>
                <textarea id="description" name="description" rows="4" required><%= event.description %></textarea>
            </div>
            <div class="form-group">
                <label for="date">Date:</label>
                <input type="date" id="date" name="date" value="<%= event.date.toISOString().split('T')[0] %>" required>
            </div>
            <div class="form-group">
                <label for="time">Time:</label>
                <input type="time" id="time" name="time" value="<%= event.time %>" required>
            </div>
            <div class="form-group">
                <label for="location">Location:</label>
                <input type="text" id="location" name="location" value="<%= event.location %>" required>
            </div>
            <div class="form-group">
                <label for="capacity">Capacity:</label>
                <input type="number" id="capacity" name="capacity" value="<%= event.capacity %>" required>
            </div>
            <div class="form-group">
                <label for="selectEvents">Select Event Type:</label>
                <select id="selectEvents" name="selectEvents" required>
                    <option value="">Select an event type...</option>
                    <option value="Tech Summit" <%= event.selectEvents === 'Tech Summit' ? 'selected' : '' %>>Tech Summit</option>
                    <option value="Hackathon" <%= event.selectEvents === 'Hackathon' ? 'selected' : '' %>>Hackathon</option>
                    <option value="Developer Conference" <%= event.selectEvents === 'Developer Conference' ? 'selected' : '' %>>Developer Conference</option>
                    <option value="AI and Robotics Expo" <%= event.selectEvents === 'AI and Robotics Expo' ? 'selected' : '' %>>AI and Robotics Expo</option>
                    <option value="Music Festival" <%= event.selectEvents === 'Music Festival' ? 'selected' : '' %>>Music Festival</option>
                    <option value="Wedding Celebration" <%= event.selectEvents === 'Wedding Celebration' ? 'selected' : '' %>>Wedding Celebration</option>
                    <option value="Science Expo" <%= event.selectEvents === 'Science Expo' ? 'selected' : '' %>>Science Expo</option>
                    <option value="Networking Mixer" <%= event.selectEvents === 'Networking Mixer' ? 'selected' : '' %>>Networking Mixer</option>
                </select>
            </div>
            <button type="submit">Update Event</button>
        </form>
    </div>
</body>
</html>
